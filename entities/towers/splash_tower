from entities.towers.tower import Tower

class SplashTower(Tower):
    cost = 90

    def __init__(self, x, y):
        super().__init__(
            x, y,
            damage=40,
            range_radius=150,
            fire_rate=60,
            cost=self.cost
        )
